"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/* eslint-disable import/no-extraneous-dependencies */
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore Import throws an error in internal CJS build, but seems to work fine after build
const reporters_1 = require("vitest/reporters");
const reporter_js_1 = require("./utils/reporter.cjs");
class LangSmithEvalReporter extends reporters_1.DefaultReporter {
    constructor() {
        super(...arguments);
        Object.defineProperty(this, "skipOnFinished", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: false
        });
    }
    async onFinished(files, errors) {
        const onFinished = reporters_1.DefaultReporter.prototype.onFinished;
        onFinished?.call(this, files, errors);
        if (this.skipOnFinished)
            return;
        await (0, reporter_js_1.printVitestReporterTable)(files, this.ctx);
    }
    async onTestRunEnd(...args) {
        super.onTestRunEnd(...args);
        this.skipOnFinished = true;
        await (0, reporter_js_1.printVitestTestModulesReporterTable)(args[0]);
    }
}
exports.default = LangSmithEvalReporter;
